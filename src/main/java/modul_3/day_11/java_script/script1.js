let car = {
    type: "sedan",
    color: "green",
    engine: 2.0
}

console.log(car.type + " " + car.color + " " + car.type)

let color = {
    red: 100,
    green: 0,
    blue: 50
}

console.log(color);

color.red = 50;
color.green = 50;

console.log(color);
